{
    "framework": "other",
    "buildCommand": "./flutter/bin/flutter build web --release --dart-define=BACKEND_URL=$BACKEND_URL",
    "outputDirectory": "build/web",
    "installCommand": "if [ ! -d \"flutter\" ]; then git clone https://github.com/flutter/flutter.git -b stable flutter; fi && ./flutter/bin/flutter doctor && ./flutter/bin/flutter config --enable-web",
    "rewrites": [
        {
            "source": "/((?!assets/|icons/|db/|manifest.json|favicon.png).*)",
            "destination": "/index.html"
        }
    ]
}
